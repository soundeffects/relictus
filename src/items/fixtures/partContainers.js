import { Fixture } from '../../structure';

export default class PartContainers extends Fixture {
  constructor() {
    super(['Lander Part Containers', 'containers']);
    this.description = "These giant aluminum bins are about as tall as two people, just as wide, and double to triple that length deep. Inside is some padding and straps probably for the safe transit of the parts inside, but most of them have been ripped or loosened somehow, letting the parts float freely.";
  }
}