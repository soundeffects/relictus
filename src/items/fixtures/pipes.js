import { Fixture } from '../../structure';

export default class Pipes extends Fixture {
  constructor() {
    super(['Pipes']);
    this.description = "These pipes are looking a little worse for wear, but most of them seem to be mostly intact.";
  }
}