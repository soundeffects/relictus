import { Fixture } from '../../structure';

export default class Scrubbers extends Fixture {
  constructor() {
    super(['Nutrient Synthesizer', 'crucible', 'synth']);
    this.description = "This large tank takes up most of the room, and it looks like a crucible. Dials for pressure, temperature, and more are slightly moving, but remaining steady. There are a few maitainence hatches that are fastened shut with bolts.";
  }
}