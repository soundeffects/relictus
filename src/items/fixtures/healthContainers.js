import { Fixture } from '../../structure';

export default class HealthContainers extends Fixture {
  constructor() {
    super(['Health Containers', 'containers']);
    this.description = "The unopened container has a red cross on it, likely indicating medical supplies. It seems to taunt you with all the  survival-worthy treasures it might hold.";
  }
}