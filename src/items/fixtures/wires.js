import { Fixture } from '../../structure';

export default class Wires extends Fixture {
  constructor() {
    super(['Wires']);
    this.description = "The wires are a tangled mess, sending sparks up from time to time. Definitely not functional.";
  }
}